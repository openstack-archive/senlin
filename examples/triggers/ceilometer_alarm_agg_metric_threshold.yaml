# Sample trigger using gnocchi aggegration by metrics alarm

trigger_type: CeilometerAggMetricsThresholdAlarm
version: 1.0

rule:
  comparison_operator: lt
  threshold: 15
  granularity: 60
  evaluation_periods: 1
  aggregation_method: avg
  repeat_actions: True
  metrics: [cpu_util]
